# Per-node model overrides for this skill.
# Copy to skills/<your_skill>/.env and fill in values.
# Empty value = fallback to CONCLAVE_DEFAULT_MODEL in root .env

CONCLAVE_INIT_MODEL=
# Add more per-node overrides as needed:
# CONCLAVE_TRIAGE_MODEL=
# CONCLAVE_ANALYZE_MODEL=
